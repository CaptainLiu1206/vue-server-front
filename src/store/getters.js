const getters = {
    userList (state) {
        return state.userList
    },
    userTotal (state) {
        return state.userTotal
    },
    user (state) {
        return state.user
    }
}

export default getters
